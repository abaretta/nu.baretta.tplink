'use strict';

function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

const TplinkConnection = require('./tplink-connection');

const TcpSocket = require('./tcp-socket');

class TcpConnection extends TplinkConnection {
  /**
   * @private
   */
  getSocket(reuseSocket) {
    var _superprop_callGetSocket = (..._args) => super.getSocket(..._args);

    return _asyncToGenerator(function* () {
      return _superprop_callGetSocket(TcpSocket, reuseSocket);
    })();
  } //   this.log.debug(`TcpConnection.getTcpSocket()`);
  //   if (reuseSocket && this.sharedSocket != null) {
  //     this.log.debug(`UdpConnection.getUdpSocket(): reusing shared socket`);
  //     if (this.sharedSocket.socket != null && this.sharedSocket.isBound) {
  //       return this.sharedSocket;
  //     }
  //     this.log.debug(`UdpConnection.getUdpSocket(): recreating shared socket`);
  //   }
  //   const tcpSocket = new TcpSocket(this.client.getNextSocketId(), this.client.log);
  //   await tcpSocket.createSocket();
  //   if (reuseSocket) {
  //     tcpSocket.unref(); // let node exit cleanly if socket is left open
  //     this.sharedSocket = tcpSocket;
  //   }
  //   return tcpSocket;
  // }


  send(payload, {
    timeout,
    reuseSocket,
    reuseSocketTimeout
  } = {}) {
    var _this = this;

    return _asyncToGenerator(function* () {
      _this.log.debug(`TcpConnection.send(%j)`, {
        payload,
        timeout
      });

      if (reuseSocketTimeout != null) {
        _this.setTimeout(reuseSocketTimeout);
      }

      let tcpSocket;
      return _this.queue.add(
      /*#__PURE__*/
      _asyncToGenerator(function* () {
        try {
          tcpSocket = yield _this.getSocket(reuseSocket);
          let response = yield tcpSocket.send(payload, _this.port, _this.host, {
            timeout
          });

          if (!reuseSocket) {
            tcpSocket.close();
          }

          return response;
        } catch (err) {
          _this.log.error(`TCP Error:`, err);

          if (tcpSocket && tcpSocket.isBound) tcpSocket.close();
          throw err;
        }
      }));
    })();
  }

  close() {
    if (this.sharedSocket && this.sharedSocket.isBound) {
      this.log.debug('TcpConnection.close()');
      this.sharedSocket.close();
    }
  }
  /**
   * @private
   */


  setTimeout(timeout) {
    if (this.timer) {
      clearTimeout(this.timer);
    }

    if (timeout > 0) {
      this.timer = setTimeout(() => {
        this.emit('timeout');
      }, timeout);
    }
  }

}

module.exports = TcpConnection;